# P13 data-platform local demo. Dummy values only.
POSTGRES_USER=data
POSTGRES_PASSWORD=data
POSTGRES_DB=data

MINIO_ROOT_USER=pfdata
MINIO_ROOT_PASSWORD=pfdata-dev-not-for-prod
MINIO_BUCKET=raw-sales

# good = seeds/orders.csv. broken = seeds/broken_orders.csv (quality gate demo).
PIPELINE_SOURCE=good
